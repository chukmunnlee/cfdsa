---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: api-gtw
  namespace: api-ns
spec:
  gatewayClassName: cilium  
  listeners:
  - name: http
    hostname: "*.192.168.39.200.nip.io"
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All

